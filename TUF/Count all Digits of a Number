class Solution {
    public int countDigit(int n) {
        // if(n == 0){
        //     return 1;
        // }
        // int count = 0;
        // n = Math.abs(n);
        // while(n > 0){
        //     count++;
        //     n = n/10;
        // }
        // return count;

        n = Math.abs(n);
        String m = Integer.toString(n);
        return m.length();
    }
}
